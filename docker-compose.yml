version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: agentrails-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: kmatth007
      POSTGRES_DB: agentrails_sandbox
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
